<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.launch_mode" >
    <application android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThem.NoActionBar">

        <!--launch mode-->
        <activity android:name="com.example.launch_mode.StartActivity"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.launch_mode.StandardActivity"
            android:launchMode="standard"/>
        <activity android:name="com.example.launch_mode.SingleTopActivity"
            android:launchMode="singleTop"/>
        <activity android:name="com.example.launch_mode.SingleTaskActivity"
            android:launchMode="singleTask"/>
        <activity android:name="com.example.launch_mode.SingleInstanceActivity"
            android:launchMode="singleInstance"/>
        <!--launch mode-->
    </application>
</manifest>
